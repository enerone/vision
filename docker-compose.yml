version: '3.8'
services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./ferreteria_ai/inventory.db:/app/inventory.db
      - ./ferreteria_ai/static/uploads:/app/static/uploads
